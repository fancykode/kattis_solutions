#include <bits/stdc++.h>

using namespace std;

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);
  cout.tie(nullptr);
  int a, b;
  cin >> a >> b;
  if (2 <= b && 2 >= a) {
    cout << "1\n2\n";
  } else {
    cout << ":(\n";
  }
  return 0;
}
